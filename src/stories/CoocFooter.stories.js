import React from "react";

import CoocFooter from "../components/CoocFooter";

export default {
  title: "Cooc/CoocFooter",
  component: CoocFooter,
  argTypes: {},
};

const Template = (args) => <CoocFooter {...args} />;

export const Default = Template.bind({});
Default.args = {};
